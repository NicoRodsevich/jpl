# Dart/Flutter build artifacts
.dart_tool/
build/
.flutter-plugins
.flutter-plugins-dependencies
.packages
*.iml
.idea/
# Local-only monorepo wiring (path overrides for local build; pub publish ignores these anyway)
**/pubspec_overrides.yaml
pubspec.lock
# Raw upstream clones (reference only)
.staging/
# workflow scratch
.jpl-salvage-workflow.js

# Estado local de publicación
.pending.txt
.cascade.log
.cron.log
run_cascade.sh
